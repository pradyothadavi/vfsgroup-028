#include "hashTable.h"

/*
Function Name: v_initializeHashTable
Description: It initializes the hash table with the required parameters
Parameters: It does not take any parameters
Return Type: It returns void
*/
void v_initializeHashTable(struct hashBucket *hashTable){

}

/*
Function Name: v_loadHashTable
Description: It loads the hash table with all the files in the file system using
             the hash function.
Parameters: It takes a single pointer, which is the pointer to the root of the 
            n-Ary Tree.
Return Type: It return's void
*/
void v_loadHashTable(struct hashBucket *hashTable,struct nAryTreeNode *sPtr_rootOfNAryTree){

    /* Traverse n-Ary Tree till you encounter a file */

    /* Auxillary traverse function which will return a file descriptor pointer */

                  3. Fetch the file name and inode number

                  4. Calculate the index

                   5. Insert into hash table (insert module)
                       i) two parameters index, file name
}


